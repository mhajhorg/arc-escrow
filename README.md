# Arc Ecrow

AI-verified escrow platform for human workers and agent workers on Arc Network.

## Getting Started

First, install Node.js if not already installed.

Then, install dependencies:

```bash
npm install
```

Run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Project Structure

- `src/app/` - Next.js app router pages and layouts
- `src/components/` - Reusable React components
- `src/lib/` - Utility functions
- `src/providers/` - Context providers for state management

## Tech Stack

- Next.js 14
- TypeScript
- Tailwind CSS
- shadcn/ui
- Framer Motion
- React Hook Form
- Zod
- TanStack Query
- wagmi
- viem
- Sonner
- Lucide React